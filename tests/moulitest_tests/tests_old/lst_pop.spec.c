#include "project.h"

UT_TEST(lst_pop)
{
	UT_ASSERT_W(0);
	// t_lst	*lst;
	// t_lst_elem *elem;
	// lst = lst_init();

	// lst_push(lst, lst_create_elem(strdup("aaa")));
	// elem = lst->elems;
	// lst_pop(lst);
	// UT_ASSERT(elem->data == NULL);
	// UT_ASSERT(lst_len(lst) == 0);

	// lst = lst_init();
	// lst_push(lst, lst_create_elem(strdup("111")));
	// lst_push(lst, lst_create_elem(strdup("222")));
	// lst_push(lst, lst_create_elem(strdup("333")));
	// UT_ASSERT(lst_len(lst) == 3);
	// lst_pop(lst);

	// UT_ASSERT(lst_get(lst, 0)->prev == NULL);
	// UT_ASSERT(strequ(lst_get(lst, 0)->data, "111"));
	// UT_ASSERT(strequ(lst_get(lst, 1)->data, "222"));
	// UT_ASSERT(strequ(lst_get(lst, 1)->prev->data, "111"));
	// UT_ASSERT(lst_get(lst, 1)->next == NULL);
	// UT_ASSERT(lst_get(lst, 2) == NULL);
	// UT_ASSERT(lst_len(lst) == 2);

	// lst_pop(lst);
	// UT_ASSERT(lst_len(lst) == 1);
	// lst_pop(lst);
	// UT_ASSERT(lst_len(lst) == 0);
}
