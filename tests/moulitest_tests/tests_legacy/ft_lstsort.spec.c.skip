#include "project.h"

// static int twl_lstcmp(t_list *lst1, t_list *lst2)
// {
// 	return (strcmp((char *)lst1->content, (char *)lst2->content));
// }

UT_TEST(twl_lstsort)
{
	// t_list	*list;

	// list = twl_lstnew("b", 10);
	// list->next = twl_lstnew("c", 10);
	// list->next->next = twl_lstnew("a", 10);

	// twl_lstsort(list, &twl_lstcmp);
	// UT_ASSERT(strcmp(list->content, "a") == 0);

	UT_ASSERT(1);
}
